{
  "name": "string-skill",
  "version": "1.0.0",
  "type": "module",
  "description": "String template management for Claude Code",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "bin": {
    "string-skill": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "build:skill": "npm run build && mkdir -p skills/string-skill/scripts && cp dist/*.js skills/string-skill/scripts/",
    "dev": "tsup --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
